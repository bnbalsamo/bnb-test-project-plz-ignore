---
name: Release

on:
  release:
    types: [published]

jobs:
  release:
    runs-on: "ubuntu-latest"
    steps:
      - uses: "actions/checkout@v2"
      - run: |
          python -m pipx install build twine invoke
          python -m invoke publish
